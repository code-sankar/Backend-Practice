<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-full bg-zinc-900 p-10  text-white">
        <div class="nav mb-10">
            <a class="text-zinc-500" href="/read">Read Users</a>
        </div>

        <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input type="text" class="px-5 py-3 border-2 border-zinc-800 rounded-lg outline-none bg-transparent"
                placeholder="name" value="<%= user.name %>">
            <input type="text" class="px-5 py-3 border-2 border-zinc-800 rounded-lg outline-none bg-transparent"
                placeholder="email" value="<%= user.email %>">
            <input type="text" class="px-5 py-3 border-2 border-zinc-800 rounded-lg outline-none bg-transparent"
                placeholder="image" value="<%= user.image %>">
            <input type="submit" class="px-5 py-3 text-black bg-yellow-500 rounded-lg ml-3 value=" Update User">
        </form>
    </div>
</body>

</html>